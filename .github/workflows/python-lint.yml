name: Run Python Lint Checks

on:
  push:
    branches:
      - main
      - frontend-rewrite
    paths:
      - 'app/**'
      - 'main.py'
      - 'requirements*.txt'
      - 'pyproject.toml'
      - '.ruff.toml'
  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - 'app/**'
      - 'main.py'
      - 'requirements*.txt'
      - 'pyproject.toml'
      - '.ruff.toml'

jobs:
  lint-python:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt', 'requirements-web.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff~=0.15.8

      - name: Run ruff lint check
        run: ruff check main.py app --config .ruff.toml
        working-directory: .
